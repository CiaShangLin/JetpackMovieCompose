package com.shang.designsystem.theme

import androidx.compose.ui.graphics.Color

val Primary = Color(0xFF3B82F6) // --primary: 藍色主色調
val OnPrimary = Color(0xFFFFFFFF) // --primary-foreground: 白色文字
val PrimaryContainer = Color(0xFFDBEAFE) // Primary的淺色容器
val OnPrimaryContainer = Color(0xFF1E3A8A) // Primary容器上的深色文字

val Secondary = Color(0xFFF1F5F9) // --secondary: 淺灰色
val OnSecondary = Color(0xFF0F172A) // --secondary-foreground: 深色文字
val SecondaryContainer = Color(0xFFE2E8F0) // Secondary的容器色
val OnSecondaryContainer = Color(0xFF334155) // Secondary容器上的文字

val Tertiary = Color(0xFF6366F1) // 紫色輔助色
val OnTertiary = Color(0xFFFFFFFF) // 白色文字
val TertiaryContainer = Color(0xFFE0E7FF) // Tertiary的淺色容器
val OnTertiaryContainer = Color(0xFF3730A3) // Tertiary容器上的深色文字

val Error = Color(0xFFEF4444) // --destructive: 紅色錯誤色
val OnError = Color(0xFFFFFFFF) // 白色文字
val ErrorContainer = Color(0xFFFEE2E2) // 錯誤色的淺色容器
val OnErrorContainer = Color(0xFFDC2626) // 錯誤容器上的深色文字

val Background = Color(0xFFFFFFFF) // --background: 純白背景
val OnBackground = Color(0xFF0F172A) // --foreground: 深色文字

val Surface = Color(0xFFFFFFFF) // --card: 卡片背景色
val OnSurface = Color(0xFF0F172A) // --card-foreground: 卡片文字色
val SurfaceVariant = Color(0xFFF8FAFC) // --muted: 淺灰變體
val OnSurfaceVariant = Color(0xFF64748B) // --muted-foreground: 中性文字色

val InverseSurface = Color(0xFF1E293B) // 反向表面色
val InverseOnSurface = Color(0xFFF1F5F9) // 反向表面文字色

val Outline = Color(0xFFE2E8F0) // --border: 邊框色
val OutlineVariant = Color(0xFFF1F5F9) // 邊框變體色

// Dark Theme Colors - 對應React v6版本的.dark CSS變數
val DarkPrimary = Color(0xFF60A5FA) // 亮藍色，在深色背景上清晰可見
val DarkOnPrimary = Color(0xFF1E3A8A) // 深藍色文字
val DarkPrimaryContainer = Color(0xFF1E40AF) // Primary的深色容器
val DarkOnPrimaryContainer = Color(0xFFDBEAFE) // Primary容器上的淺色文字

val DarkSecondary = Color(0xFF475569) // 中灰色
val DarkOnSecondary = Color(0xFFF1F5F9) // 淺色文字
val DarkSecondaryContainer = Color(0xFF334155) // Secondary的深色容器
val DarkOnSecondaryContainer = Color(0xFFE2E8F0) // Secondary容器上的淺色文字

val DarkTertiary = Color(0xFF818CF8) // 亮紫色
val DarkOnTertiary = Color(0xFF312E81) // 深紫色文字
val DarkTertiaryContainer = Color(0xFF4338CA) // Tertiary的深色容器
val DarkOnTertiaryContainer = Color(0xFFE0E7FF) // Tertiary容器上的淺色文字

val DarkError = Color(0xFFF87171) // 亮紅色
val DarkOnError = Color(0xFF7F1D1D) // 深紅色文字
val DarkErrorContainer = Color(0xFFDC2626) // 錯誤色的深色容器
val DarkOnErrorContainer = Color(0xFFFEE2E2) // 錯誤容器上的淺色文字

val DarkBackground = Color(0xFF0F172A) // --background: 深色背景
val DarkOnBackground = Color(0xFFF1F5F9) // --foreground: 淺色文字

val DarkSurface = Color(0xFF1E293B) // --card: 深色卡片背景
val DarkOnSurface = Color(0xFFF1F5F9) // --card-foreground: 淺色卡片文字
val DarkSurfaceVariant = Color(0xFF334155) // --muted: 深灰變體
val DarkOnSurfaceVariant = Color(0xFFCBD5E1) // --muted-foreground: 淺灰文字

val DarkInverseSurface = Color(0xFFF1F5F9) // 反向表面色
val DarkInverseOnSurface = Color(0xFF1E293B) // 反向表面文字色

val DarkOutline = Color(0xFF475569) // --border: 深色邊框
val DarkOutlineVariant = Color(0xFF334155) // 深色邊框變體

// 電影應用特定顏色
val MovieRatingYellow = Color(0xFFFBBF24) // 評分星星的黃色
val MovieFavoriteRed = Color(0xFFEF4444) // 收藏愛心的紅色
val MovieCardShadow = Color(0x1A000000) // 卡片陰影色 (10% 黑色透明度)
val MovieOverlay = Color(0x80000000) // 圖片遮罩色 (50% 黑色透明度)
